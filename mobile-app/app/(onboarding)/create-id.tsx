/**
 * DEPRECATED — moved to app/(auth)/create-identity.tsx
 */
import { Redirect } from 'expo-router';
export default function DeprecatedCreateId() {
  return <Redirect href="/(auth)/create-identity" />;
}
