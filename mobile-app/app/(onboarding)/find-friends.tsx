/**
 * DEPRECATED — moved to app/(auth)/find-friends.tsx
 */
import { Redirect } from 'expo-router';
export default function DeprecatedFindFriends() {
  return <Redirect href="/(auth)/find-friends" />;
}
