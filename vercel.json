{
  "installCommand": "npm install && cd mobile-app && npm install && cd ../backend && npm install",
  "buildCommand": "cd mobile-app && npx expo export --platform web",
  "outputDirectory": "mobile-app/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.ts" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
